char* get_text(size_t size); // Declaration of the method to get text from user
char* encrypt(char plain_text[], const char* keyword); // Declaration of the method to encrypt the plain text
char* decrypt(char* cipher_text, const char* keyword); // Declaration of the method to decrypt the cipher text